<p style="font-family: Arial; font-style: bold; font-size: 18px; color: red">This is all report of Group
</p>
<hr/>
Advance Search:<br/>
<%= form_for @users, url: find_report_user_path do |f|%>

  <%= f.collection_select :email,
      @users, :id, :email,include_blank: true%> 
  Day<input type='text' name='find_day' size='2' maxleng='2'/>
  Month<input type='text' name='find_month' size='2' maxleng='2'/>
  Year<input type='text' name='find_year' size='4' maxleng='4'/>
  <%=f.submit "Search", class: "btn btn-large btn-primary" %>
<% end %>

<!-- end find of users -->
<!-- find in group -->
<form action = <%=find_report_group_path%> >
  Day<input type='text' name='find_day' size='2' maxleng='2'/>
  Month<input type='text' name='find_month' size='2' maxleng='2'/>
  Year<input type='text' name='find_year' size='4' maxleng='4'/>
  <input type='submit' value ="Search" %>
</form>

<!-- end find gorup -->












<hr/>
<h2>  <%= Namegroup.find_by_sql("SELECT group_name from namegroups where group_id=#{current_user.group_id}")[0].group_name %>(ID: <p3><%=current_user.group_id%></p3>)<br/></h2>
<p3>
  Save All : >>
  <%= link_to "Excel", group_report_path(format: "xls") %>
</p3>

<% @users.each do |user|%>
<hr/>
    <%= gravatar_for user %> <p3><%= user.email%></p3> <br/><br/> 
   
    <%catalogs =Report.find_by_sql("select DISTINCT  catalog_id from reports where user_id=#{user.id}") %> <!--Number catalog for this user-->
     <% col = catalogs.count%>
     <%tmp = col-1%>
    <% if col==0%>
      Không tìm thấy nội dung nào liên quan.<br/><br/>
    <% else %><!--have report ..create table -->
      <table border='1'>
        <tr height= "50px", align = "center" bgcolor = "#CCFF33">
          <% for i in 0..tmp %>
          <td><%= Catalog.find(catalogs[i].catalog_id.to_i).name%></td>
          <% end %>
        </tr>

        <tr>
          <% for i in 0..tmp %>
          <% @count = 0 %>
          <% reports_of_user = Report.find_by_sql("select *from reports where user_id=#{user.id} and catalog_id=#{catalogs[i].catalog_id.to_i} ORDER BY created_at DESC")%>
            <td  >
              <div id="scrollcell">
                <% reports_of_user.each do |r|%>
                  
                  <ul>
                    <li><p3><%= @count+=1 %>-</p3> <%= r.content %></li>
                    <span class="file_attach">
                    <a href="/data/<%=r.file_name%>" target="_blank"><%= r.file_name %></a>
                    <span class="timestamp"><%= r.created_at.strftime("%B %e,%Y") %> (<%= time_ago_in_words(r.created_at) %>ago)</span>
                  </ul>
                <% end %>
              </div>
            </td>
          <% end %>
        </tr>
       </table>
       <br/>
       <p>
  Save:>>
  <%= link_to "Excel", member_report_path(user) %>
</p>
    <% end %>

<% end %>
